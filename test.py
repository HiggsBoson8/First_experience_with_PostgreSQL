#CREATE SELECT * FROM users 

# sudo apt upgrade
# sudo apt-get  install postgresql postgresql-contrib (sudo apt  install postgresql postgresql-contrib)
# sudo -u postgres psql postgres
# CREATE DATABASE people ;
# \l

# UPDATE users SET login = 'abay' WHERE id = 1;
# SELECT * FROM users WHERE phone LIKE '%75%';
# SELECT * FROM users WHERE login LIKE '%abay%';
# SELECT * FROM users WHERE email LIKE '%s%';

